# 旧版备份与恢复

2026-09-15 保存修改前的非多 Agent 版本。该版本已有检索和本地模型辅助识别，尚无 LangGraph 或多 Agent 协作。

- GitHub 私有仓库：`wenqizheng26/signal-formula-rag`。
- 旧版源码标签：`pre-agent-2026-09-15`。标签对应源码、依赖锁、公开公式库、测试和工程文档；不是完整运行环境。
- 本机完整备份：`E:\codex\项目\信号与AI\backups\pre-agent-20260915T081742Z`。
- 完整副本在 `project/`；`manifest.json` 逐文件记录 SHA-256；`RESTORE.md` 说明恢复步骤。共 27,063 个文件、6,868,980,308 字节，复制后的全部文件均完成 SHA-256 校验。
- 从备份目录使用本机现有 Python 执行无模型测试：82 项通过。未在本次重新运行模型推理、屏幕输入或实地传播验证。

完整备份包含本机模型、推理程序、虚拟环境、开发参考数据及旧报告。GitHub 仅保存源码等工程文件；原表、历史计算导出、模型和虚拟环境不上传。

恢复源码可检出上述标签。恢复完整本机运行环境时，先停止项目进程、另存修改后的工作目录，再按 `RESTORE.md` 将完整副本恢复到原路径。本机 `.venv` 依赖外部基础 Python，换机器仍需重新安装 Python、锁定依赖和匹配的运行资源。备份校验不能代替跨机器启动验收。

后续每个阶段单独提交，在分支上推进；阶段报告保存在 `reports/stages/`。旧报告保留本机，不作为新版本验证结论。
