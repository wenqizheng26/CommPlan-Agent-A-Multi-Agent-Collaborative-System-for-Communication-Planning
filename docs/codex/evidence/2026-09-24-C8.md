# C8 本机模型补充判断修复证据（2026-09-24）

## 现象与环境

- 工作区 `CommPlan-Agent`，修改前 HEAD `2e06a40`，与 `commplan/codex/model-eval-integration` 一致，工作树干净。未触碰 M1 工作区。
- 本机 `127.0.0.1:18081/health` 返回 `{"status":"ok"}`，`/v1/models` 含 `signal-formula-qwen3`。现有服务保持运行。
- 基线：`.venv\Scripts\python.exe -m unittest discover -s tests` 为 251 项 OK（1 skip）；`node --test tests/*.test.mjs` 为 33 项 PASS。
- 直接调用 `LocalSupplementSelector`，current 固定为“按自由空间基准计算，频率2GHz，距离1km，求路径损耗。”，candidates 按 `merge_supplement` 的 `field/evidence` 结构传入。模型在没有示例的提示词下，对 7 条输入都给出 `clarify`；原始内容见下。探针和 UTF-8 原始记录位于忽略目录 `outputs/c8_probe.py`、`outputs/c8-before.jsonl`、`outputs/c8-after.jsonl`。

## 修改前后原始模型输出

以下代码块直接取自 chat 返回的 content；每条的 `field` 和 `evidence` 也是模型原文。

### 频率改为3GHz（期望 apply）

修改前原始输出：

```json
{
  "action": "clarify",
  "fields": [
    {
      "field": "frequency_ghz",
      "evidence": "频率改为3GHz"
    }
  ]
}
```

修改后原始输出：

```json
{"action": "apply", "fields": [{"field": "frequency_ghz", "evidence": "频率改为3GHz"}]}
```

### 距离2km（期望 apply）

修改前原始输出：

```json
{
  "action": "clarify",
  "fields": [
    {
      "field": "distance_km",
      "evidence": "距离2km"
    }
  ]
}
```

修改后原始输出：

```json
{"action": "apply", "fields": [{"field": "distance_km", "evidence": "距离2km"}]}
```

### 频率设为2.4GHz（期望 apply）

修改前原始输出：

```json
{
  "action": "clarify",
  "fields": [
    {
      "field": "frequency_ghz",
      "evidence": "频率设为2.4GHz"
    }
  ]
}
```

修改后原始输出：

```json
{"action": "apply", "fields": [{"field": "frequency_ghz", "evidence": "频率设为2.4GHz"}]}
```

### 距离为3km（期望 apply）

修改前原始输出：

```json
{
  "action": "clarify",
  "fields": [
    {
      "field": "distance_km",
      "evidence": "距离为3km"
    }
  ]
}
```

修改后原始输出：

```json
{"action": "apply", "fields": [{"field": "distance_km", "evidence": "距离为3km"}]}
```

### 频率也许是3GHz（期望 clarify）

修改前原始输出：

```json
{
  "action": "clarify",
  "fields": [
    {
      "field": "frequency_ghz",
      "evidence": "频率也许是3GHz"
    }
  ]
}
```

修改后原始输出：

```json
{"action": "clarify", "fields": [{"field": "frequency_ghz", "evidence": "频率也许是3GHz"}]}
```

### 如果距离是5km（期望 clarify）

修改前原始输出：

```json
{
  "action": "clarify",
  "fields": [
    {
      "field": "distance_km",
      "evidence": "如果距离是5km"
    }
  ]
}
```

修改后原始输出：

```json
{"action": "clarify", "fields": [{"field": "distance_km", "evidence": "如果距离是5km"}]}
```

### 频率不是2GHz（期望 clarify）

修改前原始输出：

```json
{
  "action": "clarify",
  "fields": [
    {
      "field": "frequency_ghz",
      "evidence": "频率不是2GHz"
    }
  ]
}
```

修改后原始输出：

```json
{"action": "clarify", "fields": [{"field": "frequency_ghz", "evidence": "频率不是2GHz"}]}
```

## 补丁与复验

- `planning/services/supplement.py` 只改 `LocalSupplementSelector` 的 messages：明确单值的措辞增加“直接给出数值”和“改为、设为、采用、是、为”；在最后一次用户输入前加入一条 `apply`、一条 `clarify` 示例。schema、`FIELDS`、`CLAUSE`、`merge_supplement` 判断逻辑未改。
- `tests/test_planning_supplement.py` 新增两项测试：捕获 chat payload 验证正反示例和最后一条实时输入；服务层用 chat 桩验证 `apply` 真正合并、`clarify` 留待澄清。修改前 payload 测试按预期失败，修改后该文件 10 项 OK。
- 同一探针复验 7/7 与期望一致：4 条明确补充 `apply`，3 条含糊、条件或否定补充 `clarify`。
- 真实 Qwen 服务层独立数据库 `outputs/c8-service.sqlite`：任务 `d77b0642-6ebe-41e6-bf04-e844727722b2` 从完整 2 GHz、1 km FSPL 描述以 `mode='llm'` 创建，补充“频率改为3GHz”后 `turn.mode=llm_grounded`、`applied=true`、状态 `AWAITING_CONFIRMATION`。确认并计算后状态 `COMPLETED`，数值 `101.94242509439326` dB，等于 `92.4 + 20·log10(3)`。本次需求解释 `llm`、计算 `llm`、审查 `llm`，均按角色实际记录。完整回合记录见忽略目录 `outputs/c8-service-flow.json`。
- 独立原始任务 `b496df16-8c44-4029-8d9d-e8e34711df85` 用 2 GHz、1 km 确认计算，结果 `98.42059991327963` dB，仍与原值完全一致。
- 完整回归：Python 253 项 OK（1 skip）；Node 33 项 PASS；`.venv\Scripts\python.exe -m pip check` 返回 `No broken requirements found.`

## 四类状态

| 类别 | 状态 |
| --- | --- |
| 自动测试 | PASS local：Python 253 项 OK（1 skip），Node 33 项 PASS，pip check PASS。 |
| 真实模型 | PASS local：Qwen 探针 7/7，服务层合并、确认和数值复验通过。 |
| 浏览器检查 | target Chrome pending：本次未执行目标 Chrome 补测，不声明通过。 |
| Git | 仅本地提交；未推送、未开 PR、未合并。提交号以本地 Git 记录为准。 |

## 给用户的目标 Chrome 补测步骤

设置切到“本机模型”，创建完整 FSPL 任务（频率 2 GHz、距离 1 km），在“补充与修改”输入“频率改为3GHz”。确认补充已合并，重新确认任务后检查结果约 101.94 dB，并查看控制台无错误。
