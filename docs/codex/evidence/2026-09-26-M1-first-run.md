# M1 首轮验收与修复记录

## 范围与发布

用户最新要求为完成截图中全部剩余事项后再上传。当前实现仅在 `codex/m1-week3`。未推送、未开 PR、未合并、未发 Release。

独立验收题先于模型提示词阅读冻结，提交 `fa4795a`，文件 `tests/eval/m1_cases.jsonl`，SHA-256 `bebb2a38168ca5dd875cdccc50d0cd516ca32de909eb2a37644a594355e9e0e1`。

## C12 首轮实机

本机 Qwen3-Embedding-0.6B Q8，llama.cpp CPU 服务，1024 维。登记三个 ITU PDF 与三个模拟 Markdown 文档。首次索引为 318 个文档片段，冷预热约 213.37 秒，缓存约 7.4 MB。固定八个查询分别执行词项、向量、混合，共 24 次，均未降级。混合检索约 88–109 毫秒；向量约 148–269 毫秒。原始无全文结果在本机 `outputs/eval/c12-first-run.json`。

查询覆盖自由空间损耗、等效地球半径、k 因子、海面多径、衰落余量、站点坐标和两种模拟电台。出处包括 ITU 页码及模拟文档章节。随后站址模拟文档新增环境列，因此后续索引哈希与该首次记录不同。

只读审查发现并修复：HTTP 总等待预算、外部资源目录误判未安装、暂时离线后不能恢复。有限等待覆盖完整 HTTP 调用，超时退到词项检索，并限制一个未完成查询，防止请求堆积；失败预热五秒后可重试。

## C13 首轮

首轮输出保留于本机 `outputs/eval/c13-first-run/`，不覆盖。结果：主场景 6/12，变体 7/10，在线与确定性输出逐位一致 6/6。保护用例 H1–H4 共 8/8，通过记录为 `outputs/eval/c13-hard-first.jsonl`。保护测试使用故意损坏的输入或替身输出，不能当成真实模型准确率。

只回答用例缺少的“自由空间基准”条件，不代答模型漏掉的目标或数值。冻结用例中的 `fspl` 在运行器中显式映射到现有卡编号 `fspl_ghz`，没有修改原用例。

首轮不满足验收要求。主要问题包括：“是否满足”错误触发数值不确定；问号后的余量数字被错误归到频率；电台型号 XX-100 后的顿号被错当离散频率；模型目标摘录核对不接受明确余量要求；模型计划漏掉视距步骤。已据此新增反例测试与失败原因纠错，不通过时仍按程序拼链并明确标注。

首轮运行期间有本地源码与模拟文档修订，不作为最终固定版本证据。后续运行器记录 build fingerprint，逐例核对源码与知识库未变，发生变更即中止。

## 设计同步

另一个工作区新增设计提交 `472e2b7`，本轮核对并采纳其计算 Agent 设计：参数齐全时一次调用完成计划和适用性评估；每步一句理由；评估只作提示，不阻止确认；确认后的数值仍由程序执行。其文档中的“先推送分支”不执行，以用户最新要求为准。

## 当前仍需完成

修复后完整真实验收、最后全量回归、页面全部场景与三种宽度、真实抽取和连续追问演示、源码包验证、最终审查与 GitHub 发布。此记录不是完成声明。
