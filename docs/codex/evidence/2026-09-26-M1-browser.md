# 最终代码的浏览器复查（2026-09-26）

- 工具：内置浏览器，不是目标 Chrome。
- 代码：`claude/calc-plans`，含页面修正 `27b26d6`。
- 运行：工作台用临时数据库，端口 18095；本机 9B 模型，词项检索。

## 演示问题 A→B（本机模型）

问题：“按自由空间基准，A 站到 B 站用 XX-100 电台、2 GHz，要留 10 dB 余量，能通吗？不能的话发射功率至少要多大？”

- **待确认**：需求理解用时 72.5 s。9B 偏慢的原因见[最终运行记录](2026-09-26-M1-final-run.md)的显存一节。
- **计划卡**：
  - 标题：“计划 · 专业计算 Agent 编写 · 程序校验通过”。
  - 目标：“链路余量 ≥ 10 dB；不满足时求最小发射功率”；条件为自由空间模型、自由空间基准。
  - 适用性评估为“可以确认”，含对题、适用性、假设三条。
  - 步骤的来源标签为“站点库 · 模拟”。
- **确认后**：61.6 s 完成，11/11 校验通过。
  - 余量 5.93 dB，“要求 ≥ 10 dB · 不满足”，下面是程序结论。
  - 反求：发射功率至少 41.07 dBm，超过额定 37 dBm；两侧复核、32 次迭代、曲线与要求线都有。
- **审查**：调用超时一次。页面显示“模型调用失败 1 次”，流程图标“部分调用已降级”，所以没有模型答复卡。这与 C13 最终运行中的 6 条情况相同。有模型答复与“请核对”的页面见 [C14 证据](2026-09-26-C14.md)。
- **依据页**：“文档依据”列出 3 段（XX-200、XX-100 手册），带章节定位与“模拟数据”。
  - 发现每段都显示“来源链接格式无效”：模拟文档的来源是仓库内路径，不是网址。
  - 已修正（`27b26d6`），刷新后显示“本地资料：knowledge/documents/simulated/…”；网址来源仍是可打开的链接。
- **宽度**：1430×804 与 760 宽都查看了；760 宽无横向滚动。控制台无错误。

## 已知不足

- 切块时只有标题的一行也成为一块（如“# XX-100 手册（模拟）”），会占用一个送入审查的名额。改切块规则会改变 C12 已记录的检索结果和旧任务的重放，本次不改，列入下一步。
- 目标 Chrome 验收未做。
