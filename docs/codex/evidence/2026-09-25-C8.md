# C8：本机生成模型切换验证（2026-09-25）

工作区 `CommPlan-Agent-M1-codex`，分支 `codex/m1-week2`，起点 `ff7c3e5`。未推送、未合并。`models/` 为指向主工作区模型目录的被忽略目录联接。

## 权重与运行时

- 从 [Unsloth 官方文件页](https://huggingface.co/unsloth/Qwen3.5-9B-GGUF/blob/3885219b6810b007914f3a7950a8d1b469d598a5/Qwen3.5-9B-Q4_K_M.gguf) 下载单一文件 `Qwen3.5-9B-Q4_K_M.gguf`。Hugging Face API 的 LFS 大小为 5,680,522,464 字节，SHA256 为 `03b74727a860a56338e042c4420bb3f04b2fec5734175f4cb9fa853daf52b7e8`；本机 `Get-FileHash` 与大小逐项相同。没有下载 mmproj。
- 本机 `llama.cpp-b10950`、`Vulkan1`（RTX 4060 Laptop，`nvidia-smi` 显示 8,188 MiB）、`-ngl 99`、`-c 16384`、18081 端口。先停止同一工作区启动的 4B，再启动 9B；两者未同时运行。
- 详细加载日志：`offloaded 33/33 layers to GPU`、`Vulkan1 KV buffer size = 512.00 MiB`、`Vulkan1 RS buffer size = 50.25 MiB`、`Vulkan1 compute buffer size = 112.02 MiB`。日志也有 `CPU_Mapped model buffer size = 545.62 MiB` 和 `Vulkan_Host compute buffer size = 32.02 MiB`，因此只确认全层 GPU 与日志中无 CPU 计算缓冲/回落信息，不能从这些日志证明绝无 CPU 操作。参考的 [llama.cpp #19957](https://github.com/ggml-org/llama.cpp/issues/19957) 讨论 35B MoE；本次 9B 实际输出正常，未出现该问题描述的乱码或设备丢失。
- 三条固定中文提示均输出正常。流式首字延迟依次为 0.584、0.260、0.242 秒；总时长 1.703、0.922、0.936 秒；按生成 token / 首字后时长估算 41.11、40.83、41.77 token/s。`response_format: json_schema` 且 `strict: true` 的请求返回可解析的双站点 JSON 对象。原始结果位于被忽略的 `outputs/c8-probe-result.json`，完整运行日志位于 `runtime/c8-9b-verbose.err.log`。
- 注册表默认 `qwen35-9b-q4`，两项 4B 档案保留。启动器从注册表读取权重、别名、端口、上下文和设备，不再读取 `runtime_config.json` 的 `generation`。手动验证后停止模型，再由 `start_commplan.ensure_model` 启动 9B，`model_ready` 与 `probe_model` 均返回就绪。

## 36 条真实模型评测

同一分支、同一评测集 SHA256 `c2ff30e1c9a3236b13ae9045b36296920e386720baa2ba7dd8b4dc90e4003537`，分别运行 4B 和 9B。先运行 4B 作为逐案基线，再停服务并运行 9B。

| 档案 | 六类合同字段 | 结构化成功 | 降级 | p50 / p95 |
| --- | --- | --- | --- | --- |
| Qwen3 4B Q4_K_M | 各 36/36 | 33/33 | 0 | 1.923 / 5.281 s |
| Qwen3.5 9B Q4_K_M | 各 36/36 | 33/33 | 0 | 2.517 / 8.299 s |

按相同 36 个 id 逐案比较，合同字段、结构化成功和运行健康状态的退化项均为空。评测得分包含程序核对与降级机制，不能表述为纯模型准确率。原始 JSON 位于被忽略的 `outputs/c8-4b-baseline/`、`outputs/c8-9b-eval/`。A1 记录只保存汇总，故以本次重跑的同分支 4B JSON 为逐案基线；A1 原有汇总也为 36/36、33/33、0 降级。

## 自动回归

本工作区 Python `unittest discover -s tests -q`：287 项，`OK (skipped=1)`；Node `node --test tests/*.test.mjs`：41 项通过；主工作区 `.venv` 的 `pip check`：`No broken requirements found.`；`git diff --check` 无空白错误。首次完整 Python 回归有 2 个旧默认模型硬编码断言失败，按 C8 新默认合同更新测试后，定向 3 项与完整 287 项重跑通过。
