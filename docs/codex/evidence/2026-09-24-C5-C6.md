# C5/C6 本机验证证据（2026-09-24）

候选分支：`codex/model-eval-integration`。C5：`41d18b0`。旧网页清理：GitHub `main` 合并 `1d97f6d`。集成合并：`7771c2c`。本记录不替代目标 Chrome 验收或独立 Reviewer。

## C5 固定案例评测

命令：`python -B -X utf8 scripts/eval_models.py --models qwen3-4b-q4 qwen3-4b-q4-fast-fail`，使用 `tests/eval/requirements_cases.jsonl` 的 30 条固定案例；报告写入 Git 忽略的 `outputs/eval/2026-09-24T020548Z-*.json`。

| 指标 | 确定性 | Qwen 标准 | Qwen 快速失败 |
| --- | ---: | ---: | ---: |
| 案例数 | 30 | 30 | 30 |
| 参数/目标/条件/状态/诊断/提问匹配 | 各 30/30 | 各 30/30 | 各 30/30 |
| 模型结构化成功/实际尝试 | 不适用（0/0） | 25/27 | 25/27 |
| 降级案例 | 0 | 2 | 2 |
| 需求代理 p95 | 6 ms | 2004 ms | 2197 ms |
| 需求代理 token（prompt/completion） | 不适用 | 13270/1053 | 13270/1053 |

两个 Qwen 档案的 30/30 匹配包含程序校验与确定性回退；它不是纯模型准确率。两个档案的 2 次降级案例均记录 `MODEL_OUTPUT_INVALID`，各有 6 次失败尝试。离线模型由评测器标为 `unscored`，不伪装为通过或失败。

## C6 合并后回归与实际链路

- 修复评测单测的 CI 环境假设后，`python -B -X utf8 -m unittest discover -s tests -p 'test_*.py'`：251 tests OK，1 skip（Windows symlink 权限）。
- `node --test tests/*.test.mjs`：33 pass，0 fail。`python -m pip check`：No broken requirements found。
- 独立任务库 `outputs/c6-integration.sqlite`；脚本及原始结果 `outputs/c6_integration.py`、`outputs/c6-{normal,hybrid,offline}.json` 均在 Git 忽略目录，未纳入发行包。
- 正常 Qwen + 审查角色快速失败档案：任务 `c6-normal-2bfec9db`，`COMPLETED`，词项检索；需求与计算模型成功，审查模型两次结构化失败后明确降级，`runtime_health=degraded`。
- BGE 预热后混合检索 + Qwen：任务 `c6-hybrid-a6d3ad61`，`COMPLETED`，`state.retrieval.mode_used=hybrid`，BGE `ready`（预热 13156 ms），三个模型角色均完成，`runtime_health=ready`。报告中的 `component_modes.retrieval=lexical_fallback` 指可信候选的词项证据门禁；实际检索方式以 `state.retrieval` 为准，向量相似度单独不准入公式。
- 在核对 18081 PID 的可执行文件、权重和端口均属于项目内资源后，短暂停止该进程：任务 `c6-offline-72d40757`，`COMPLETED`，三个模型调用均记录 `offline`，解释为确定性、计算和审查为确定性回退，`runtime_health=degraded`。然后从项目内资源重启；`/health` 返回 `ok`，`/v1/models` 包含 `signal-formula-qwen3`。
- 三次运行发布的 `path_loss_db` 原值均为 **98.42059991327963 dB**；它来自受保护的确定性计算，不能用来证明模型数学能力。

## C6 源码包

在干净提交 `3ad5797` 运行 `scripts/build_planning_release.py --require-clean`，然后运行 `scripts/validate_planning_release.py`：84 个文件，`source_dirty=false`，fingerprint `38f0790cf7ad5cad7fe9`，SHA-256 `37CA8522D96121D9EF2C50D99FE18EE26BB3D9C79CC36E4EE3E325C445A0B041`；校验器解压、指纹和 HTTP 创建/确认/重启恢复均 PASS。ZIP 无模型权重、llama runtime、数据库、日志、PID 或旧版根目录 `web/`。

另将该 ZIP 解压到全新目录，运行 `setup_planning.cmd`（`COMMPLAN_PYTHON` 指向已验证的 Python 3.12）、`pip check` 与解压目录自带 venv 的 HTTP smoke，全部 PASS。文档提交后将从最终干净分支头再次构建，最终 SHA-256 以该构建为准；Stage 5 仍须从被接受的 `main` 重新构建。

首次 GitHub [run 35947605972](https://github.com/wenqizheng26/CommPlan-Agent-A-Multi-Agent-Collaborative-System-for-Communication-Planning/actions/runs/35947605972) 的 `legacy-full` 因单测把未安装模型误判为离线而失败；`e301ecc` 让单测分别模拟“已安装但离线”和“未安装”，没有修改评测器的运行分类。随后 [run 35948390923](https://github.com/wenqizheng26/CommPlan-Agent-A-Multi-Agent-Collaborative-System-for-Communication-Planning/actions/runs/35948390923) 的 `planning-minimal`、`legacy-full` 均 PASS。

最终代码提交 `8314fb1` 的 [run 35948954929](https://github.com/wenqizheng26/CommPlan-Agent-A-Multi-Agent-Collaborative-System-for-Communication-Planning/actions/runs/35948954929) 两个作业均 PASS。用户随后确认目标 Chrome 对新候选 `http://127.0.0.1:18095` 的七项流程和 V4 §29 附加检查全部通过；该结论为用户手动验收，不冒充实施者浏览器观测。

当前仍需本验收记录后续提交的 CI 和实施团队外独立审查；Stage 5 的 PR、接受到 `main`、main-derived ZIP 和 tag 均未执行。
