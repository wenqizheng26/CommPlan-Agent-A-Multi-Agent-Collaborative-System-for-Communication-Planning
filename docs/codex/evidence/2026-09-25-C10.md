# C10 模拟事实库与查询验证（2026-09-25）

工作区：`CommPlan-Agent-M1-codex`，分支 `codex/m1-week2`。只交付事实库、模拟原文、查询和知识快照；未接入需求解析或页面流程。

## 数据与合同

- 7 条模拟站点、2 条模拟设备。三份文档首行标“模拟数据，仅供演示”，JSON 每项有文档路径、版本和具体行或章节。D 站 `antenna_m` 保留 `null`。
- `FactService.find_site/find_device` 优先名称精确，其次别名精确，最后规范化包含匹配；返回同级全部候选并按 id 排序。“港口站”返回东港与西港两项，不能自动选。空名称及超过 50 字报 `FACT_QUERY_INVALID`。
- 仅返回 `verified` 记录；不联网。查询结果有来源定位。校验 WGS84 坐标、设备数值与频段，文档与结构化值由测试逐项核对。
- `snapshot_for` 的来源清单纳入事实 JSON 与模拟 Markdown 的 SHA256。改任一文件，快照改变；确认路径比较快照，报 `KNOWLEDGE_CHANGED`。源根目录在任务流程中显式传递，避免临时或独立工作区误读其他目录。

## 附录 A 数值复核

使用 C9 的 WGS84 弦长、视距、FSPL、接收功率和余量卡。频率 2 GHz、XX-100 两端、馈线损耗各 2 dB、额外损耗 0、预留 0。距离及视距单位 km，余量单位 dB。下表由程序公式重新计算；设计附录只给两位小数的估算。

| 链路 | 弦长 | 视距 | 余量 | 处理 |
| --- | ---: | ---: | ---: | --- |
| A→B | 34.064379 | 43.166169 | 5.933391 | 不满足 10 dB |
| A→C | 85.065609 | 40.991370 | — | 超出视距，预算阻断 |
| A→D | — | — | — | 天线高度缺失，追问 |
| A→F | 11.981648 | 38.522861 | 15.009069 | 满足 10 dB |
| A→东港 | 25.977277 | 40.991370 | 8.287528 | 选择实体后不满足 |
| A→西港 | 22.013016 | 40.991370 | 9.725809 | 选择实体后不满足 |

弦长/视距与 C9 实测相同，余量与附录 A 的两位小数估算差异小于 0.01 dB。超视距 A→C 的预算中间算术不作为可发布链路结果。

## 验证

- `python -m unittest discover -s tests -q`：299 tests，OK，1 skip（本次改动后的全量运行；随后传递源根目录的小修由 34 项定向测试通过）。
- `python -m unittest tests.test_m1_facts tests.test_planning_loop tests.test_planning_roles -q`：34 tests，OK。
- `node --test tests/*.test.mjs`：41 pass。
- `python -m pip check`：No broken requirements found。

限制：模拟数值不是实测站址或真实设备规格；本任务只提供查询能力，不代表模型已能自动理解地点、设备或完成 A3 流程接入。文档哈希变化触发快照失效，但用户确认和参数来源展示仍由后续 A3 完成。
