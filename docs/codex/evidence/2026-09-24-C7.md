# C7：计算链评测与真实 Qwen 回归（2026-09-24）

工作区 `CommPlan-Agent-M1`，分支 `claude/calc-plans`，起点 `1cb2310`。`git worktree list --porcelain` 已核对工作区、分支及 HEAD；主工作区 `CommPlan-Agent` 保持在 `codex/model-eval-integration`，本任务没有在那里改文件。M1 工作区的 `models` 是指向主工作区模型目录的被忽略目录联接。Qwen 127.0.0.1:18081 的 `/health` 为 `ok`，`/v1/models` 含 `signal-formula-qwen3`。

## 自动测试与六条用例

修改前：`python -B -X utf8 -m unittest discover -s tests` 为 262 tests OK（1 skip）；`node --test tests/*.test.mjs` 为 36 pass。新增 6 行到 `tests/eval/requirements_cases.jsonl`，保留原有分类集合；`tests/test_eval_models.py` 的案例数量及状态匹配断言改为 36。六条均先以 `RequirementsAgent(selector=False)` 逐条运行，并核对完整参数、目标、条件、状态、指定诊断码及是否提问；之后完整确定性评测六类评分均为 36/36。

| 新增 id | 确定性状态 | 目标 | 指定诊断码 | 核对 |
| --- | --- | --- | --- | --- |
| `chain_margin_complete` | `AWAITING_CONFIRMATION` | `link_margin` | 无 | PASS |
| `chain_power_complete` | `AWAITING_CONFIRMATION` | `received_power` | 无 | PASS |
| `chain_margin_missing_budget` | `AWAITING_INPUT` | `link_margin` | `MISSING_INPUT` | PASS |
| `chain_margin_noise` | `NEEDS_MODEL` | `link_margin`, `thermal_noise` | `UNSUPPORTED_SCOPE` | PASS |
| `chain_margin_interval` | `AWAITING_INPUT` | `link_margin` | `PLAN_DOMAIN_UNSUPPORTED` | PASS |
| `chain_margin_negative_loss` | `AWAITING_INPUT` | `link_margin` | `INPUT_DOMAIN_INVALID` | PASS |

完整用例原文及完整参数期望保存在 JSONL；`MARGIN`、`POWER` 原文直接来自 `tests/test_calculation_plans.py`。评测原始 JSON 位于被忽略的 `outputs/eval/2026-09-24T051029Z-*.json`。

最终回归：Python 262 tests OK（1 skip）；Node 36 pass、0 fail；`python -m pip check` 输出 `No broken requirements found.`。评测用例是数据行，因此 Python unittest 数量仍是 262。

## 三组评测

| 档案 | 案例及六类评分 | 模型结构化成功 / 实际尝试 | 降级案例 | 需求代理 p95 | prompt / completion token |
| --- | --- | --- | ---: | ---: | ---: |
| 确定性 | 36；各 36/36 | 0/0 | 0 | 5 ms | 不适用 |
| `qwen3-4b-q4` | 36；各 36/36 | 29/33 | 4 | 4004 ms | 18334 / 1289 |
| `qwen3-4b-q4-fast-fail` | 36；各 36/36 | 29/33 | 4 | 4396 ms | 18334 / 1289 |

两档 Qwen 的 36/36 包含程序约束和降级回退，不能视为纯模型准确率。每档 4 个降级案例：`missing_condition`、`missing_target`、`link_margin_missing_budget`、`chain_margin_missing_budget`；均为 `MODEL_OUTPUT_INVALID`，每档累计 12 次失败尝试。确定性 p95 取同次三档命令的报告；单独确定性命令也得到各 36/36。

## 与 C6 逐条对比

C6 原始报告为主工作区被忽略的 `outputs/eval/2026-09-24T020548Z-{qwen3-4b-q4,qwen3-4b-q4-fast-fail}.json`，汇总见 [C5/C6 证据](2026-09-24-C5-C6.md)。C6 有 30 条，本分支有 36 条；按 id 共有 29 条。两档 Qwen 对这 29 条逐条比较 `checks`、通过状态、实际状态、运行健康、解释模式、是否尝试模型、结构化成功、降级原因及诊断码，**变化与退化均为无**。原有 FSPL 案例也在这 29 条中，目标匹配、结构化成功与降级情况均未变差；该结论限于这些实际运行的固定案例。

29 条共同 id：`complete_2g_1km`、`complete_unit_conversion`、`complete_4_5g_200m`、`complete_3g_1km`、`free_space_model`、`missing_distance`、`missing_frequency`、`missing_both`、`missing_condition`、`missing_target`、`conflicting_frequency_text`、`conflicting_frequency_manual`、`conflicting_condition_manual`、`conflicting_target_manual`、`interval_frequency_tolerance`、`interval_frequency_range`、`interval_distance_range`、`interval_reversed`、`choices_frequency`、`choices_distance`、`choices_frequency_three`、`no_units`、`no_distance_unit`、`sea_actual`、`scattering`、`below_far_field`、`manual_target_condition`、`manual_frequency`、`manual_all`。纯 FSPL 请求的候选现在包含 `fspl_ghz` 与 `received_power`，上述实跑表明固定 FSPL 用例结果未退化。

第 30 条存在分支语义变化，不能按 id 当成同一案例：C6 的 `link_margin` 原文“按自由空间基准计算链路余量，频率2GHz，距离1km。”期望及实际均为 `NEEDS_MODEL`，`runtime_health=ready`，模型未调用，因此**没有模型原始输出**。M1 原有的同原文 `link_margin_missing_budget` 期望及实际均为 `AWAITING_INPUT`；两档模型均三次输出不合结构后降级。此项是计算链扩展带来的状态变化，也应作为真实模型改进点保留，不掩盖为“完全无变化”。本次另加的 `chain_margin_missing_budget` 也出现同类降级。两档在 `link_margin_missing_budget` 的独立复现原始诊断保存于 `outputs/c7-link-margin-raw-*.json`；该复现与 36 条正式评测分开。

模型原始输出（`link_margin_missing_budget` 独立复现，按尝试顺序）：

- 标准档第 1 次：`{"selected_ids":["link_margin","fspl_ghz"],"targets":[{"id":"link_margin","evidence":"计算接收电平高于接收门限的余量，再扣除用户指定的工程预留量"},{"id":"fspl_ghz","evidence":"自由空间点对点通信的路径损耗基准"}],"conditions":[{"id":"free_space_reference","evidence":"按自由空间基准计算"}]}`
- 标准档第 2、3 次均为：`{"selected_ids":["link_margin","fspl_ghz"],"targets":[{"id":"link_margin","evidence":"计算接收电平高于接收门限的余量，再扣除用户指定的工程预留量"}],"conditions":[{"id":"free_space_reference","evidence":"按自由空间基准计算"}]}`
- 快速失败档第 1 次：`{"selected_ids":["link_margin","fspl_ghz"],"targets":[{"id":"link_margin","evidence":"计算接收电平高于接收门限的余量，再扣除用户指定的工程预留量"},{"id":"fspl_ghz","evidence":"按自由空间基准计算"}],"conditions":[{"id":"free_space_reference","evidence":"按自由空间基准计算"}]}`
- 快速失败档第 2、3 次与标准档第 2、3 次相同。

这些输出的 `selected_ids` 含 `fspl_ghz`，但 `targets` 没有相应合法原文目标证据；程序拒绝并降级。未修改 `planning/`、`formula_rag/` 或 `knowledge/`，交由后续实施者判断模型候选或提示词是否应改。

## 内置浏览器页面检查

M1 服务从本工作区启动在 `http://127.0.0.1:18094/`，任务库为被忽略的 `outputs/c7-ui.sqlite3`；界面设置切至“本机模型 · Qwen3 4B · 标准”，点击“链路预算 ↗”，完成“开始筹划 → 计划页签核对 → 确认并计算”。任务 id：`66d3d0c5-149a-49cc-b3f0-1ba59c31d2f8`，最终 `COMPLETED`。计划为 `fspl_ghz → received_power → link_margin`。结果显示路径损耗 **118.42 dB**、接收信号电平 **−72.42 dBm**、链路余量 **17.58 dB**；结果页签九项程序校验均显示 `✓`。

截图：[计划页签](2026-09-24-C7-plan.png)、[三步结果](2026-09-24-C7-result.png)、[九项校验](2026-09-24-C7-validations.png)。这是 Codex 内置浏览器对本机服务的检查，**不是目标 Chrome 验收**。

## 边界与临时环境

最初临时探针写入失败，仅因 M1 工作区尚无被忽略的 `outputs/` 目录；创建后重跑通过，没有项目检查失败。临时脚本、评测报告、原始模型复现和 SQLite 均位于被忽略的 `outputs/`。本任务仅在 `claude/calc-plans` 本地提交；不推送、不合并。
