# 一个月验收点（M1）

状态：用户 2026-09-24 确认。演示或验收节点：2026-10-24 前。
定位：本机离线的通信筹划助手，同时作为单位内部工具与科研原型。按“先打地基再扩用途”推进：通用计算引擎 → 公式入库管道 → 选型 → 组网与覆盖。M1 只覆盖前两项的一部分。
相关规格：[CALCULATION_PLANS](CALCULATION_PLANS.md)、[KNOWLEDGE_FACTS](KNOWLEDGE_FACTS.md)。

## 演示问题

> A 站到 B 站用 XX-100 电台、2 GHz，要留 10 dB 余量，能通吗？不能的话发射功率至少要多大？

| 能力 | 期望行为 |
| --- | --- |
| 听懂 | 在模拟站点库里识别 A、B，取出坐标和天线高度；在模拟设备库里识别 XX-100，取出发射功率、天线增益、接收灵敏度；没人提的量（如馈线损耗）列为假设 |
| 拆解 | 计划为：用坐标算直线距离 → 视距检查 → FSPL → 接收功率 → 余量 → 余量不足时反求最小发射功率；程序检查计划，用户确认一次 |
| 讲清楚 | 回答“余量 x dB，不满足 10 dB；发射功率至少 y dBm”；每一步都能展开，每个输入都标明来源（原文 / 站点库 / 设备库 / 假设 / 上一步） |

站点和设备数据是模拟的，标注 `simulated: true`，页面上显示“模拟数据”。以后换成真实数据时只换数据文件，不改程序。设备库沿用 KNOWLEDGE_FACTS 的事实机制，新增 `device` 类型。

## 三档验收

| 档次 | 内容 | 通过标准 |
| --- | --- | --- |
| 保底 | v0.1.0 发布门禁通过；通用引擎能算出参数全部写在原文里的链路余量；页面展示计划并确认计划 | 余量与手算差 ≤ 0.01 dB；现有 FSPL 任务结果与 v0.1.0 逐位一致 |
| 目标 | 演示问题完整跑通：站点库与设备库补全、视距检查、反求、模型写计划（出错或离线时改用程序拼链） | 主场景加下列 5 个变体全部通过；反求与闭式解的相对误差 ≤ 1e-6；模型离线时数值与在线相同 |
| 加分 | 从一份公开 ITU-R 文档抽取一张公式草稿，审核后入库并参与计算；记录模型计划与程序拼链的一致率 | 草稿未审核前不能参与计算；评测结果落到文件 |

目标档变体：
1. 缺少天线高度时追问高度。
2. 两站超出视距时拦下，不给损耗值。
3. 用户给出的发射功率与设备库不一致时提问。
4. 同名站点存在歧义时让用户选择。
5. 余量本来就够时直接回答“能通”，不做反求。

## 周排期

| 周 | 内容 | 到达 |
| --- | --- | --- |
| 1 | 独立审查与 v0.1.0 发布（由用户安排审查人）；通用引擎正向计算、计划格式与校验、程序拼链、计划确认 | 保底 |
| 2 | 反求与复核；模拟站点库与设备库；直线距离与视距卡片 | — |
| 3 | 模型写计划（重写一次，然后回退）；计划确认页与逐步结果页 | 目标 |
| 4 | 公式草稿与审核的最小版本；评测数据；演练与缓冲 | 加分 |

每周结束都应该是一个可以演示的完整状态；截止日做到哪一档就演示哪一档，不演示半成品。

## 进度

- **第 1 周（2026-09-24，提交 9a69535）**：通用引擎正向计算已完成。
  - 程序按公式卡的输出名和输入名反向拼出计算链，例如“链路余量”自动拼成 FSPL → 接收电平 → 余量三步；每个输入都绑定到已确认的参数，或者上一步的结果。
  - `check_report` 独立重建同一份计划，校验一致后才允许确认。执行时逐步计算，校验时每一步都用独立实现的公式复算。
  - 单步 FSPL 保留 v0.1.0 的原路径，结果形状与数值逐位一致。
  - 页面“计划”页签逐步列出输入来源，结果页有逐步数值；缺少的链路预算参数用中文列出。
- **本机验证**：
  - Python 262 项通过（1 项 skip），Node 36 项通过。
  - 在内置浏览器中用确定性模式完整跑通链路预算示例：余量 17.58 dB，与手算一致，9 项校验全部通过，控制台没有错误。
- **C7（Codex，6297076）**：新增 6 条计算链评测用例，共 36 条；两档真实 Qwen 与 C6 共有的 29 条无退化；纯 FSPL 请求多出“接收电平”候选后，目标识别没有变化。
- **第 1 周收尾（2026-09-24）**：C7 暴露的问题和页面流程里的缺口已补齐，详见 [收尾记录](../codex/evidence/2026-09-24-M1-week1-closeout.md)。
  - 模型依据只能从原文分句里选（解码约束），纠错反馈说明哪一项错、为什么错；两档 Qwen 从 29/33 次结构化成功、4 条降级，变为 33/33、0 条降级，与 C7 逐条比较没有变差。
  - 链路预算参数可以逐项回答，也可以在“补充与修改”里一次写明；超出定义域的值（如负馈线损耗）也能直接改正。
  - 补充判断在本机模型模式下原本一律回答“需要澄清”（v0.1.0 同样如此），加入正反示例后能正确合并。
  - 审查输入去掉重复的修改前后原文，多轮补充后不再超出 4096 上下文，审查角色不再退回默认结论。
  - Python 269 项通过（1 项 skip），Node 36 项通过；本机模型模式下的页面全流程（缺参 → 逐项回答 → 补充 → 确认 → 三步结果，三个模型角色均为 `llm`）已在内置浏览器中检查。
  - 以下尚未进行：目标 Chrome 验收、独立审查。
- **工作台可读性改版（2026-09-24）**：按 [WORKBENCH_UI](WORKBENCH_UI.md) 重排页面，改动在 `planning/web`，后端只加一个只读接口；数值与确认流程不变。
  - 左栏收全部输入（待补充问题一次列全，补充框固定在底部）；流程图保留主位并兼做唯一进度；右栏标题一句话说明当前要做什么，确认条固定在底部。
  - 右栏计划卡就是第 3 周计划确认页与逐步结果页的骨架：每步带公式出处，每个输入带来源标签。
  - 公式页逐步显示三条登记公式；新增只读接口 `/api/formula-cards`，卡片内容哈希与任务证据一致才显示。确认快照与数值不变。
  - Python 270 项通过（1 项 skip），Node 41 项通过；内置浏览器在 1430×804、1280×720、760 宽下检查了新建、解析、待补充、待确认、计算、完成、编辑原文、逐项回答、补充，控制台无错误。目标 Chrome 未验。
- **已知限制**：
  - 区间和候选值只用于单步 FSPL；多步计划要求每个输入都是单值。
- **保底档还差**：v0.1.0 的独立审查与发布（由用户安排）。

## 边界

- 选型、组网与覆盖、地形数据都不在 M1 内。
- 新功能在独立分支 `claude/calc-plans` 上开发，不影响正在审查的 v0.1.0 候选；v0.1.0 发布后再合入。
- 交付时分开说明自动测试、真实模型、浏览器验收和 Git 发布状态；内置浏览器的结果不能当作目标 Chrome 验收。
