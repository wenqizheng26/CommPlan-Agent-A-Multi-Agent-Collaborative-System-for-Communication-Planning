# 工作台界面（M1 可读性改版）

状态：用户 2026-09-24 逐项确认。只改 `planning/web`；数值、命令、确认流程不变。

## 布局

| 栏 | 职责 | 内容 |
| --- | --- | --- |
| 左 · 对话 | 所有输入 | 新任务：需求、示例、高级选项（手工参数、目标、条件）。任务开始后：只读原文（可“编辑原文”）、补充记录、待补充表单（一次列全）、底部固定补充框 |
| 中 · 流程 | 唯一的进度显示 | 五段进度 + 图例 + 架构流程图（内容与字号不变）；点节点弹出小卡片，不换右栏 |
| 右 · 计划/结果 | 核对与结果 | 标题行一句话说明当前要做什么；主卡为计划或结果；参数溯源、公式、依据三个入口；确认条固定在底部，只在待确认时出现 |

- 顶栏：当前任务一行、模型、历史任务、“⋯”（导出 JSON、性能、按编号恢复、取消任务）、新建。去掉状态徽标和顶部进度栏。
- 900–1349 宽：流程图 + 右栏，右栏顶部切换“对话 | 计划”。900 以下：单栏，计划 → 流程 → 对话，底部固定操作条。

## 计划卡（第 3 周“计划确认页 / 逐步结果页”的骨架）

- 目标、条件各一行；每步一行：序号、名称、出处、输出；展开后每个输入带来源标签（原文 / 手工 / 上一步 / 待补充 / 冲突）。
- 待确认、待补充时步骤展开；已完成时收起，行内显示结果。
- 模型假设默认收起并写明条数；结果页“适用范围”只露第一条。
- 第 3 周接入：站点库、设备库（标“模拟”）、假设值、计划来源、反求曲线。

## 文案

短标签，专业词保留（出处、校验项数、Agent 名只在流程图），不写长段说明；只写代码真实做的事。示例：“缺 7 项参数”“3 步计算链 · 待确认”“完成 · 9/9 校验通过”“规则接管 · 数值不变”。英文参数名显示时一律换成中文。
