# 通信公式 RAG 交接

## 2026-09-15：旧版备份、GitHub 与 P1

旧版已完整复制并校验；源码标签为 `pre-agent-2026-09-15`，私有仓库为 [wenqizheng26/signal-formula-rag](https://github.com/wenqizheng26/signal-formula-rag)。详见 [备份与恢复](BACKUP.md)。后续开发在 `codex/p1-applicability` 分支进行，未修改 Git 全局身份。

P1 已完成，规则版本 `p1-1.0.0`：
- 海上例题说明当前模型覆盖缺口，保留已经识别的频率和距离。
- 明显不适用的自由空间距离/波长组合不给正式损耗，下游随之暂缓；有效独立结果仍可返回。
- 负 dBm 按相对 1 mW 的功率电平解释；负、零、正余量使用未舍入数值分别判定预算门限关系。
- 接收门限要求用户明确确认噪声参考。目前只开放标准 290 K 路线，非标准温度和系统总噪声路线暂缓，P2 再扩展。确认来源不接受模型推断，不自动填入 290 K 或 −174。
- HTTP 接口和 JSON 保存保留确认来源、规则版本及结果解释。旧完整示例增加明确的噪声参考确认；原始数值保持不变。

验证：101 项无模型测试通过，包含新增 19 项适用性、边界、HTTP 与保存测试；[阶段报告](reports/stages/p1-applicability.json)记录完整计算节点、测试输出与文件哈希。没有调用真实模型或控制屏幕，也没有改变网页布局、公式常量、显示位数、原表或历史结果 JSON。人工按输入操作仍待后续专项验收。

下一项为 P2 七公式与单位版本，之后依次 P3 精度、P4 表单确认、P5 LangGraph 与多 Agent、P6 候选比较、P7 分层验收。不能把 P1 完成视为多 Agent 已接入。下面内容按日期保留历史状态；“尚未提交”等旧说明只描述当时情况。


## 2026-09-15：清理被替代的旧版本

已按用户要求清理旧设计、旧协作交接、旧图稿及废弃 Visio 脚本；有效协议已并入 [当前需求基线](docs/requirements.md)。当前只采用 [v1.4 流程图](docs/diagrams/2026-09-15/通信筹划协同流程_v1.4.md)，科学审查、P1 实施计划、应用与历史验证证据保留。删除清单与引用检查见 [清理记录](reports/docs_cleanup_2026-09-15.json)。下方首次同步的文件计数是清理前的历史检查结果。

## 2026-09-15：当前工程需求已同步

当前需求以 [工程需求基线 R1](docs/requirements.md)为准，目标流程采用 [v1.4 图稿](docs/diagrams/2026-09-15/通信筹划协同流程_v1.4.md)。本轮只修改工程文档，应用源码、公式库、依赖和旧验收报告保持不变。

- 已确认范围：多 Agent + LangGraph；自然语言先解析回填；用户核对模型、参数、约束与可调域后计算；先生成参数组合，达标方案并列呈现取舍，用户选择。
- 三个专业角色统一为“需求与知识 Agent”“计算规划 Agent”“验证与解释 Agent”，另有 Orchestrator Agent 总控。四角色共用本地能力，专业数值和约束判定由程序完成。
- 已补齐：任务分类、有限候选域、发布审查条件、版本失效、状态权限、重复提交与中断恢复、本地保存与 JSON 导出区别、批量预算计量及分层验收。
- 已消除冲突选择：不采用“不引入 LangGraph”或简化线性流程，也不把“23 例通过”解释为全部完整计算；对应过时设计文件已在后续清理中移除。
- 尚未实现：科学缺陷修复、先解析后确认的新表单、LangGraph 工作流、四角色协作、候选生成与比较。本轮在项目虚拟环境确认 LangGraph 尚未安装；没有运行本地模型或模拟页面输入。
- 下一个项目是 [P1 适用范围与结果解释](docs/superpowers/plans/2026-09-14-01-applicability.md)，开工前按需求基线复核；后续按 P2—P7 逐项实施。

核对结果见 [工程文档同步检查](reports/engineering_docs_sync_2026-09-15.json)。以下为 2026-09-14 的实现与验收记录；当前需求、已知缺陷和历史通过项应分开理解。

## 2026-09-14：已有实现交接

交付日期：2026-09-14。项目路径：`E:\codex\项目\信号与AI\signal-formula-rag`。

## 既有实现

已跑通本地中文输入 → BGE 与词项混合检索 → Qwen3 识别目标和传播条件 → 程序独立核验原文、参数、单位及适用条件 → 按依赖顺序计算 → 显示数值、LaTeX 公式、出处与缺项。模型不能替换参数、提供最终数值或执行生成代码。流程图见 [docs/system-flow.md](docs/system-flow.md)。

知识库当前为 7 条公开公式：自由空间损耗、单程最大多普勒频移上界、热噪声功率、热噪声谱密度、接收门限、接收电平、链路余量。公式集合独立折叠；结果公式后的链接可以展开集合、定位和高亮对应定义，再返回结果。补充参数采用短标签与单位；手动纠正目标和传播条件放在可选区域。

用户已明确参考表只用于开发对照，不能计入运行系统、不能据此补值。已删除运行公式库中的表格来源与备注，并移除原表页面/API。开发副本与复算报告保留，原件未修改。缺少相对速率、参考噪声、损耗等信息时，系统要求用户明确输入。

运行应用不需要付费 API、账号或订阅。模型、推理程序、字体和依赖已准备在本机。应用代码 MIT；Qwen Apache-2.0；BGE、llama.cpp 和 KaTeX 为 MIT。已有 Windows 和显卡驱动不属于本项目开源承诺，详见 `THIRD_PARTY.md`。

## 使用

双击 `启动.cmd`，或在项目目录执行：

```powershell
& '.\.venv\Scripts\python.exe' -X utf8 launch.py
```

页面：http://127.0.0.1:18080/ 。模型服务：127.0.0.1:18081。进程在本机后台运行；重复启动检查并复用。日志在 `runtime/`，资源配置在 `runtime_config.json`。

可粘贴“参数（单位）数值”的多行文本，同时请求多个待求量。计算不完整时，独立且条件明确的结果先显示，其余项目列出需要补充的信息。模型推断会显示依据；仍可人工纠正，不能保证任意自由文本语义均识别正确。

完整显式输入示例在 [examples/complete_budget.txt](examples/complete_budget.txt)。其中所有噪声、增益单位、损耗和预留量均是该示例明确输入，不是运行默认值。程序得到：

| 结果 | 程序值 |
| --- | --- |
| 自由空间基本传输损耗 | 91.48485018878651 dB |
| 接收信号电平 | −51.48485018878651 dBm |
| 接收门限 | −62 dBm |
| 电平余量 | 10.51514981121349 dB |

点击“保存结果 JSON”后，本机服务将原始计算记录写入 `outputs/`，页面显示完整路径。文件包含未按页面小数位舍入的程序值、公式版本、输入证据及缺项信息。服务器只接受已有记录的 ID，不接受客户端指定数值或文件路径。进程重启或最近 64 条记录之外的旧结果需要重新计算后保存。

本轮浏览器实测保存文件：`outputs/计算结果-20260914T065808Z-42c864d07d604540907e499f01abd081.json`；已读取并核对上述四项数值。

## 历史验证证据

| 项目 | 2026-09-14 的历史结果 | 文件 |
| --- | --- | --- |
| 单元测试 | 82 项通过，覆盖数值、单位、依赖计算、缺项、模型证据与越权、表达式安全、公式展示、API 和保存 | `reports/final_checks.json` |
| 真实本地 RAG | 23 个案例的预设断言通过：12 个 ok、1 个 partial、5 个 needs_input、2 个 not_applicable、3 个 invalid_input；不表示 23 题全部完整计算 | `reports/acceptance.json` |
| 浏览器交互 | 公式渲染、集合定位与返回、缺项提示、完整计算、短参数标签和本机 JSON 落盘通过；检查时未观察到控制台错误 | `reports/browser_qa.json` |
| JavaScript | 页面脚本语法检查通过 | `reports/final_checks.json` |
| 原表开发复算 | 16/16 个公式与缓存数值一致，最大绝对差 0；不作为运行查询数据 | `reports/workbook_audit.json` |
| 模型资源 | 版本、文件校验和、显卡/资源与本地模型响应记录 | `runtime_config.json`、`reports/runtime_smoke.json`、`reports/environment.json` |

离线验证使用 Python 审计钩子禁止非回环连接和外部 DNS，23 个案例无外部连接尝试；llama.cpp 使用 `--offline`。没有进行物理拔网测试。浏览器核验覆盖本机桌面主要流程，不代表所有移动端和浏览器均已验收。

## 数学与物理范围

- 原表副本 SHA256：`996e0ae207d41ec09a635adc47eb27d25cd4067671539f80d191bff69b9d1ec1`。
- 原表 C11/D11 的多普勒损耗 `10*LOG10(COS(...))` 缺少适用依据，仅保留开发复算。运行系统不采用该式，也不把多普勒频移当成损耗。该项需要波形、补偿策略和经审核的损耗模型；相对速度缺失也会追问。
- 自由空间损耗采用 ITU-R P.525-5 式(6)的舍入常数 92.4；与精确光速推导的常数约差 0.048 dB。自由空间仅为明确选择的模型基准，视距或海面环境本身不能证明适用。
- 最大多普勒公式使用精确光速和单程一阶上界；不支持雷达往返频移或任意径向几何。
- 接收门限需要显式噪声谱密度或参考温度，不默认采用 −174 dBm/Hz；比特率不能拿噪声带宽替代。噪声系数与参考温度的含义需一致，防止重复计入噪声。
- 天线增益需明确 dBi/dBd；只有 dB 时需澄清。平台漂移损耗应纳入哪一项不明确时需澄清，不能自动相加或填零。
- 参数解析限于登记字段及支持的表达。复杂多链路、范围、冲突或含糊文本需明确输入。程序计算正确不等于实地传播、覆盖率或可靠度已经验证。

## 后续添加公式

按 `README.md` 使用 `examples/new_formula.json` 和 `scripts/import_formula.py`。新增记录先进入 draft，审核公开来源、单位、物理条件与独立数值样例后准入。程序样例校验不能代替专业审核。知识库哈希变化后重建本地索引，不需要重新训练模型。

新参数可以在页面规范字段中填写；若要自由文本识别新参数、判断新环境或构建新依赖链，需同步扩展程序及回归案例。禁止直接执行模型临时编写的公式或代码。

## 环境与维护

- 已验证：Python 3.12.14；RTX 4060 Laptop，约 8 GB 显存；llama.cpp b10950 / Vulkan1；Qwen3-4B Q4_K_M；BGE `bge-small-zh-v1.5`；KaTeX 0.18.7。
- 精确版本、revision、路径、许可和 SHA 见配置、报告及 `web/assets/katex/manifest.json`。CPU 备用参数 `--cpu` 本轮未实测。
- 当前 `.venv` 依赖此电脑基础 Python 的绝对路径。迁移机器需准备 Python、驱动及锁定依赖；不是原样复制即可启动的便携包。
- `scripts/setup_runtime.py --offline` 验证现有资源；应用启动不下载。模型、运行时和导出结果不进入 Git。
- 本地分支 `codex/local-formula-rag`；尚未创建提交，未配置 Git 作者身份，没有推送或发布。

重跑：

```powershell
& '.\.venv\Scripts\python.exe' -X utf8 -m unittest discover -s tests -v
& '.\.venv\Scripts\python.exe' -X utf8 scripts/evaluate.py
```

真实 RAG 评估需要本地模型服务。源码、公式或界面变更后，验证报告中的文件哈希可用于判断证据是否仍匹配当前版本。
