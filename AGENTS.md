# 通信筹划工程工作约定

接管或查询进度时参考 `docs/codex/NEXT_ACTION.md`；验收证据位于 `docs/codex/evidence/`。旧交接与 WBS 是阶段记录，不能单独证明当前完成状态。

修改图状态、确认或持久化恢复时，按需使用 `.agents/skills/langgraph-workflow/SKILL.md`。其他任务按需要选用技能，不预设固定的设计、计划、测试或审查技能链。

当前正式计算范围是声明自由空间假设后的单链路路径损耗，不能代表实际海面或散射传播。复用 `formula_rag` 和公式库；专业数值由确定性工具计算并通过适用性与结果校验。

输入变更使旧确认与结果失效；确认当前版本后才能计算。任务、幂等回执和 checkpoint 保持 SQLite 主事务一致，activity 仅用于观察。

保留已有未提交修改。交付时区分自动测试、真实模型、浏览器验收与 Git 发布状态。
